# Example only — the live workflow lives at:
#   .github/workflows/deploy-pages.yml
#
# Production deploy:
# - Injects Firebase from GitHub Actions secrets (never commit keys)
# - Fails if required secrets are missing
# - Strips local adminPassword from public config
# - Excludes internal docs (FIREBASE.md, PRODUCTION.md, docs/)
#
# See PRODUCTION.md for the full go-live checklist.
